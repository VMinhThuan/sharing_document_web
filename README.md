# AI Learning Document Sharing System

Hệ thống chia sẻ tài liệu học tập được hỗ trợ bởi AI.

## 📁 Cấu trúc dự án

Tất cả source code, dependencies và file cấu hình đã được đặt trong thư mục `frontend/`:

```
sharing_document_web/
└── frontend/              ← TẤT CẢ ở đây
    ├── src/
    ├── node_modules/
    ├── package.json
    ├── package-lock.json
    ├── vite.config.js
    ├── tailwind.config.js
    ├── postcss.config.js
    ├── index.html
    └── README.md
```

## 🚀 Cách chạy ứng dụng

### Bước 1: Di chuyển vào thư mục frontend

```bash
cd frontend
```

### Bước 2: Cài đặt dependencies (nếu chưa có)

```bash
npm install
```

### Bước 3: Chạy ứng dụng

```bash
npm run dev
```

Ứng dụng sẽ chạy tại: `http://localhost:5173`

Xem file `frontend/README.md` hoặc `frontend/HUONG_DAN.md` để biết hướng dẫn chi tiết.

## ⚠️ Lưu ý

- Tất cả source code và dependencies nằm trong thư mục `frontend/`
- Phải chạy các lệnh npm từ **bên trong thư mục frontend**
